node_modules/
dist/
.dev.vars
.wrangler/
.env
.env.local
.env.production
.env.staging
*.log
.DS_Store
.playwright-mcp/
tests/load/results.json
# 調査用の使い捨てスクリプト（prod 接続を含むことがある。リポジトリに残さない）
_*_tmp.ts
# LINE トークン更新の実状態ファイル（env ごとの実行時アーティファクト。雛形は
# scripts/line-token-state.json.example を追跡する。末尾が .json.example のものは除外されない）
scripts/line-token-state.*.json
